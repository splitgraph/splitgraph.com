export const meta = {id: "engine-stop", title: "sgr engine stop", sidebarTitle: "engine stop", description: "Stop a Splitgraph engine."};

```sgr engine stop [OPTIONS] [NAME]```

Stop a Splitgraph engine.

This is a wrapper around the corresponding Docker command.
