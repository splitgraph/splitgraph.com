export const meta = {id: "engine-log", title: "sgr engine log", sidebarTitle: "engine log", description: "Get logs from a Splitgraph engine."};

```sgr engine log [OPTIONS] [NAME]```

Get logs from a Splitgraph engine.

### Options

  * **`-f, --follow`**: Stream logs
