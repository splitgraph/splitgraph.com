export const meta = {id: "cloud-token", title: "sgr cloud token", sidebarTitle: "cloud token", description: "Output an up-to-date access token for a remote."};

```sgr cloud token [OPTIONS] ```

Output an up-to-date access token for a remote.

### Options

  * **`--remote TEXT`**: Name of the remote registry to use.
