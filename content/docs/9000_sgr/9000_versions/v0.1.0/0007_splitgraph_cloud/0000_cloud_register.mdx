export const meta = {id: "cloud_register", title: "cloud register"};

## cloud register

```sgr register [OPTIONS] ```

Register the user on splitgraph.com, obtain a set of machine credentials
and configure the data.splitgraph.com engine.

### Options

  * **`--username TEXT`**: 
  * **`--password TEXT`**: 
  * **`--email TEXT`**: 
  * **`--remote TEXT`**: Name of the remote cloud engine to register on.
  * **`--accept-tos`**: Accept the registry's Terms of Service
