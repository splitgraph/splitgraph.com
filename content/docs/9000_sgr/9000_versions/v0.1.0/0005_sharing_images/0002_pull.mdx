export const meta = {id: "pull", title: "pull"};

## pull

```sgr pull [OPTIONS] REPOSITORY```

Pull changes from an upstream repository.

### Options

  * **`-d, --download-all`**: Download all objects immediately instead on checkout.
