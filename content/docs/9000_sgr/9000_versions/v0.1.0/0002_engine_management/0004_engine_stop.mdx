export const meta = {id: "engine_stop", title: "engine stop"};

## sgr engine stop

```sgr stop [OPTIONS] [NAME]```

Stop a Splitgraph engine.

This is a wrapper around the corresponding Docker command.
