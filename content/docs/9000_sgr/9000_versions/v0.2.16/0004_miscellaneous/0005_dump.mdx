export const meta = {id: "dump", title: "sgr dump", sidebarTitle: "dump", description: "Dump a repository to SQL."};

```sgr dump [OPTIONS] REPOSITORY```

Dump a repository to SQL.

### Options

  * **`--exclude-object-contents`**: Don't dump the commands needed to recreate objects required by the repository.
