export const meta = {id: "engine_start", title: "sgr engine start", sidebarTitle: "engine start"};

```sgr engine start [OPTIONS] [NAME]```

Start a Splitgraph engine.

This is a wrapper around the corresponding Docker command.
