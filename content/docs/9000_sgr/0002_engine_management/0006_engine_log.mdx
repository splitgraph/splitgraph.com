export const meta = {id: "engine_log", title: "sgr engine log", sidebarTitle: "engine log"};

```sgr log [OPTIONS] [NAME]```

Get logs from a Splitgraph engine.

### Options

  * **`-f, --follow`**: Stream logs
