export const meta = {id: "cloud_register", title: "sgr cloud register", sidebarTitle: "cloud register"};

```sgr cloud register [OPTIONS] ```

Register the user on a Splitgraph registry.

By default, this registers the user on data.splitgraph.com,
obtains a set of machine (API) credentials for the client to communicate
with the registry and configures the data.splitgraph.com engine.

### Options

  * **`--username TEXT`**: 
  * **`--password TEXT`**: 
  * **`--email TEXT`**: 
  * **`--remote TEXT`**: Name of the remote registry to register on.
  * **`--accept-tos`**: Accept the registry's Terms of Service
