export const meta = {id: "cloud_readme", title: "sgr cloud readme", sidebarTitle: "cloud readme"};

```sgr cloud readme [OPTIONS] REPOSITORY README```

Upload or overwrite a README to a previously-pushed Splitgraph repository.

The README should be a file in Markdown format.

### Options

  * **`--remote TEXT`**: Name of the remote registry to use.
