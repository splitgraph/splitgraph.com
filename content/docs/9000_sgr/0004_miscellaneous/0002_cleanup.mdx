export const meta = {id: "cleanup", title: "sgr cleanup", sidebarTitle: "cleanup", description: "Prune unneeded objects from the engine."};

```sgr cleanup [OPTIONS] ```

Prune unneeded objects from the engine.

This deletes all objects from the cache that aren't required by any local repository.
