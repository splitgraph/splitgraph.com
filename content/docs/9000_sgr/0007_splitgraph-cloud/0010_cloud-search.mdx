export const meta = {id: "cloud-search", title: "sgr cloud search", sidebarTitle: "cloud search", description: "Search for a repository on the Splitgraph registry."};

```sgr cloud search [OPTIONS] QUERY```

Search for a repository on the Splitgraph registry.

For more advanced search, including filtering by topics, go to the registry's website itself.

### Options

  * **`--remote TEXT`**: Name of the remote registry to use.
  * **`--limit INTEGER RANGE`**: Number of results to return
