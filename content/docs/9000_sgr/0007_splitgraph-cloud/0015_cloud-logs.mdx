export const meta = {id: "cloud-logs", title: "sgr cloud logs", sidebarTitle: "cloud logs", description: "Get ingestion job logs."};

```sgr cloud logs [OPTIONS] REPOSITORY TASK_ID```

Get ingestion job logs.

Output the logs for an ingestion job into a repository on Splitgraph Cloud.
Use `sgr cloud status` to get a list of ingestion jobs and their recent task IDs.

### Options

  * **`--remote TEXT`**: Name of the remote registry to use.
