export const meta = {id: "cloud-download", title: "sgr cloud download", sidebarTitle: "cloud download", description: "Download query results from Splitgraph."};

```sgr cloud download [OPTIONS] QUERY [OUTPUT_PATH]```

Download query results from Splitgraph.

This runs a query on Splitgraph Cloud and exports the results into a csv.gz format.

### Options

  * **`--remote TEXT`**: Name of the remote registry to use.
  * **`--file-format [csv]`**: 
